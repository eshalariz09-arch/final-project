apiVersion: tekton.dev/v1beta1
kind: PipelineRun
metadata:
  generateName: tax-calculator-pipeline-run-
spec:
  pipelineRef:
    name: tax-calculator-pipeline
  params:
    - name: registryNamespace
      value: eshalariz09-tax
  workspaces:
    - name: source
      persistentVolumeClaim:
        claimName: tax-calculator-pvc
